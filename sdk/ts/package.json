{
  "name": "@symbioticfi/hints-sdk",
  "version": "0.1.0",
  "description": "TypeScript helpers for generating and selecting Symbiotic hints via simulations.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "viem": "^2.9.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.3.1",
    "solc": "^0.8.29"
  }
}
